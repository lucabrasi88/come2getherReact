import React from 'react';
import ReactDOM from 'react-dom';

function CreateButton(){
    var buttonElement = <button className ="createButton"> Create event</button>;
    return buttonElement;
}

export default DbHandler;
